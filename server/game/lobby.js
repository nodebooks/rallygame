const uuid = require('uuid4');
const Race = require('./race');
const WebSocket = require('ws');

let  _playerCount = 0;

class Lobby {
  constructor() {
    console.log("TODO: lobby");
  }
}

module.exports = Lobby;